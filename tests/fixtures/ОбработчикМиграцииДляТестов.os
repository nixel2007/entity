// Обработчик миграции для тестов: регистрирует вызовы своих методов в журнале.

Перем Версия;
Перем МинимальнаяВерсия;
Перем Журнал Экспорт;

Процедура ПриСозданииОбъекта(Знач ПВерсия = "", Знач ПЖурнал = Неопределено, Знач ПМинимальнаяВерсия = "")
	Версия = ПВерсия;
	МинимальнаяВерсия = ПМинимальнаяВерсия;
	Если ПЖурнал = Неопределено Тогда
		Журнал = Новый Массив;
	Иначе
		Журнал = ПЖурнал;
	КонецЕсли;
КонецПроцедуры

Функция ВерсияСхемы() Экспорт
	Возврат Версия;
КонецФункции

Функция МинимальнаяВерсияСхемы() Экспорт
	Возврат МинимальнаяВерсия;
КонецФункции

Процедура ПередМиграцией(МенеджерСущностей, ПланМиграции) Экспорт
	Журнал.Добавить(СокрЛП("ПередМиграцией " + Версия));
КонецПроцедуры

Процедура ПослеМиграции(МенеджерСущностей, ПланМиграции) Экспорт
	Журнал.Добавить(СокрЛП("ПослеМиграции " + Версия));
КонецПроцедуры
