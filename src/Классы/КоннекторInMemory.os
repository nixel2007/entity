#Использовать "../internal"
#Использовать fluent

// Для хранения статуса соединения
Перем Открыт;
// Соответствие: имя таблицы - таблица значений с данными.
Перем КешТаблиц;
// Соответствие: имя таблицы - последний выданный идентификатор.
Перем СчетчикиИдентификаторов;
// БлокировкаЧтенияЗаписи - Защищает таблицы и счетчики: они общие у всех коннекторов подключения.
Перем Блокировка;
// Рефлектор - Читает и заполняет поля сущностей по именам колонок модели.
Перем Рефлектор;
// НаблюдателиСущностей - Наблюдатели пула, которым сообщается о каждой операции с таблицами.
// Неопределено, пока коннектор работает вне пула.
Перем Наблюдатели;
// Строка - Строка подключения, с которой открыт коннектор.
Перем СтрокаСоединенияКоннектора;

// Конструктор объекта АбстрактныйКоннектор.
//
&Реализует("АбстрактныйКоннектор")
&Реализует("НаблюдаемыйКоннектор")
Процедура ПриСозданииОбъекта()
	Открыт = Ложь;
	Рефлектор = Новый Рефлектор;
	СтрокаСоединенияКоннектора = "";
КонецПроцедуры

// Передает реестр наблюдателей, которым коннектор сообщает об операциях с таблицами.
//
// Параметры:
//   ПНаблюдатели - НаблюдателиСущностей - Реестр наблюдателей пула.
//
Процедура УстановитьНаблюдателей(ПНаблюдатели) Экспорт
	Наблюдатели = ПНаблюдатели;
КонецПроцедуры

// Описывает соединение для атрибутов событий.
//
// Возвращаемое значение:
//   Структура - Система "inmemory", пустой адрес, нулевой порт и БазаДанных - строка подключения.
//
Функция ОписаниеСоединения() Экспорт
	Возврат СтрокиСоединения.ОписаниеБезСервера("inmemory", СтрокаСоединенияКоннектора);
КонецФункции

// Открыть соединение с БД.
//
// Параметры:
//   СтрокаСоединения - Строка - Строка соединения с БД.
//   ПараметрыКоннектора - Массив - Дополнительные параметры инициализации коннектора.
//
Процедура Открыть(СтрокаСоединения, ПараметрыКоннектора) Экспорт // BSLLS:UnusedParameters-off
	СтрокаСоединенияКоннектора = СтрокаСоединения;
	КешТаблиц = ХранилищеВПамяти.КешТаблиц(СтрокаСоединения);
	СчетчикиИдентификаторов = ХранилищеВПамяти.СчетчикиИдентификаторов(СтрокаСоединения);
	Блокировка = ХранилищеВПамяти.Блокировка(СтрокаСоединения);
	Открыт = Истина;
КонецПроцедуры

// Закрыть соединение с БД.
//
Процедура Закрыть() Экспорт
	Открыт = Ложь;
КонецПроцедуры

// Получить статус соединения с БД.
//
//  Возвращаемое значение:
//   Булево - Состояние соединения. Истина, если соединение установлено и готово к использованию.
//       В обратном случае - Ложь.
//
Функция Открыт() Экспорт
	Возврат Открыт;
КонецФункции

// Начинает новую транзакцию в БД.
//
Процедура НачатьТранзакцию() Экспорт
	ВызватьИсключение "Не поддерживается";
КонецПроцедуры

// Фиксирует открытую транзакцию в БД.
//
Процедура ЗафиксироватьТранзакцию() Экспорт
	ВызватьИсключение "Не поддерживается";
КонецПроцедуры

// Отменяет открытую транзакцию в БД.
//
Процедура ОтменитьТранзакцию() Экспорт
	ВызватьИсключение "Не поддерживается";
КонецПроцедуры

// Создает таблицу в БД по данным модели.
//
// Параметры:
//   ОбъектМодели - ОбъектМодели - Объект, содержащий описание класса-сущности и настроек таблицы БД.
//
Процедура ИнициализироватьТаблицу(ОбъектМодели) Экспорт

	Событие = НачатьЗапрос("CREATE TABLE", ОбъектМодели);
	Блокировка.Запись().Заблокировать();

	Попытка
		СоздатьТаблицу(ОбъектМодели);
	Исключение
		Блокировка.Запись().Разблокировать();
		ЗавершитьЗапрос(Событие, ИнформацияОбОшибке());
		ВызватьИсключение;
	КонецПопытки;

	Блокировка.Запись().Разблокировать();
	ЗавершитьЗапрос(Событие);

КонецПроцедуры

// Начинает событие запроса для наблюдателей пула.
//
// Параметры:
//   ОперацияСУБД - Строка - Операция события.
//   ОбъектМодели - ОбъектМодели - Модель таблицы.
//
// Возвращаемое значение:
//   СобытиеСущностей, Неопределено - Событие; Неопределено вне пула или без наблюдателей.
//
Функция НачатьЗапрос(Знач ОперацияСУБД, ОбъектМодели)

	Если Наблюдатели = Неопределено Тогда
		Возврат Неопределено;
	КонецЕсли;

	Возврат Наблюдатели.НачатьЗапрос(ОперацияСУБД, ОбъектМодели.ИмяТаблицы());

КонецФункции

// Завершает событие запроса для наблюдателей пула.
//
// Параметры:
//   Событие - СобытиеСущностей, Неопределено - Событие из НачатьЗапрос.
//   ИнформацияОбОшибке - ИнформацияОбОшибке, Неопределено - Ошибка операции.
//   КоличествоСтрок - Число, Неопределено - Число прочитанных строк.
//
Процедура ЗавершитьЗапрос(Событие, Знач ИнформацияОбОшибке = Неопределено, Знач КоличествоСтрок = Неопределено)

	Если Событие = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Наблюдатели.ЗавершитьЗапрос(Событие, ИнформацияОбОшибке, КоличествоСтрок);

КонецПроцедуры

// Создает таблицу в кеше подключения, если ее там еще нет.
//
// Параметры:
//   ОбъектМодели - ОбъектМодели - Объект, содержащий описание класса-сущности и настроек таблицы БД.
//
Процедура СоздатьТаблицу(ОбъектМодели)
	ИмяТаблицы = ОбъектМодели.ИмяТаблицы();

	Если КешТаблиц.Получить(ИмяТаблицы) = Неопределено Тогда
		Таблица = Новый ТаблицаЗначений();

		Таблица.Колонки.Добавить("_Идентификатор");
		Таблица.Колонки.Добавить("_Сущность");

		Таблица.Индексы.Добавить("_Идентификатор");
		
		КешТаблиц.Вставить(ИмяТаблицы, Таблица);
	КонецЕсли;

КонецПроцедуры

// Сохраняет сущность в БД.
//
// Параметры:
//   ОбъектМодели - ОбъектМодели - Объект, содержащий описание класса-сущности и настроек таблицы БД.
//   Сущность - Произвольный - Объект (экземпляр класса, зарегистрированного в модели) для сохранения в БД.
//
Процедура Сохранить(ОбъектМодели, Сущность) Экспорт

	Событие = НачатьЗапрос("INSERT", ОбъектМодели);
	Блокировка.Запись().Заблокировать();

	Попытка
		ЗаписатьСущность(ОбъектМодели, Сущность);
	Исключение
		Блокировка.Запись().Разблокировать();
		ЗавершитьЗапрос(Событие, ИнформацияОбОшибке());
		ВызватьИсключение;
	КонецПопытки;

	Блокировка.Запись().Разблокировать();
	ЗавершитьЗапрос(Событие);

КонецПроцедуры

// Записывает сущность в таблицу кеша.
//
// Параметры:
//   ОбъектМодели - ОбъектМодели - Объект, содержащий описание класса-сущности и настроек таблицы БД.
//   Сущность - Произвольный - Объект (экземпляр класса, зарегистрированного в модели) для сохранения в БД.
//
Процедура ЗаписатьСущность(ОбъектМодели, Сущность)

	ИмяТаблицы = ОбъектМодели.ИмяТаблицы();

	Таблица = КешТаблиц.Получить(ИмяТаблицы);

	Если Таблица = Неопределено Тогда
		ВызватьИсключение "Таблица " + ИмяТаблицы + " не найдена";
	КонецЕсли;

	Идентификатор = ОбъектМодели.ПолучитьЗначениеИдентификатора(Сущность);
	Если НЕ ЗначениеЗаполнено(Идентификатор) Тогда
		
		Если ОбъектМодели.Идентификатор().ТипКолонки <> ТипыКолонок.Целое Тогда
			Сообщение = СтрШаблон(
				"Ошибка при сохранении сущности с типом %1.
				|Генерация идентификаторов поддерживается только для колонок с типом ""Целое""",
				ОбъектМодели.ТипСущности()
			);
			ВызватьИсключение Сообщение;
		КонецЕсли;

		Идентификатор = СледующийИдентификатор(ИмяТаблицы);

		ОбъектМодели.УстановитьЗначениеКолонкиВПоле(
			Сущность,
			ОбъектМодели.Идентификатор().ИмяКолонки,
			Идентификатор
		);

	КонецЕсли;

	УчестьВыданныйИдентификатор(ИмяТаблицы, Идентификатор);

	Если ТипЗнч(Идентификатор) = Тип("Число") Тогда
		Идентификатор = Формат(Идентификатор, "ЧГ=");
	КонецЕсли;

	СтрокаТЗ = Таблица.Найти(Идентификатор, "_Идентификатор");

	Если СтрокаТЗ = Неопределено Тогда
		СтрокаТЗ = Таблица.Добавить();
	КонецЕсли;

	СущностьВБД = Новый (ОбъектМодели.ТипСущности());

	Для Каждого Колонка Из ОбъектМодели.Колонки() Цикл
		Значение = ОбъектМодели.ПолучитьПриведенноеЗначениеПоля(
									Сущность, 
									Колонка.ИмяПоля);
		Рефлектор.УстановитьСвойство(СущностьВБД, Колонка.ИмяПоля, Значение);
	КонецЦикла;

	СтрокаТЗ._Идентификатор = Идентификатор;
	СтрокаТЗ._Сущность = СущностьВБД;
	
КонецПроцедуры

// Удаляет сущность из таблицы БД.
//
// Параметры:
//   ОбъектМодели - ОбъектМодели - Объект, содержащий описание класса-сущности и настроек таблицы БД.
//   Сущность - Произвольный - Объект (экземпляр класса, зарегистрированного в модели) для удаления из БД.
//
Процедура Удалить(ОбъектМодели, Сущность) Экспорт

	Событие = НачатьЗапрос("DELETE", ОбъектМодели);
	Блокировка.Запись().Заблокировать();

	Попытка
		УдалитьСущность(ОбъектМодели, Сущность);
	Исключение
		Блокировка.Запись().Разблокировать();
		ЗавершитьЗапрос(Событие, ИнформацияОбОшибке());
		ВызватьИсключение;
	КонецПопытки;

	Блокировка.Запись().Разблокировать();
	ЗавершитьЗапрос(Событие);

КонецПроцедуры

// Удаляет строку сущности из таблицы кеша.
//
// Параметры:
//   ОбъектМодели - ОбъектМодели - Объект, содержащий описание класса-сущности и настроек таблицы БД.
//   Сущность - Произвольный - Объект (экземпляр класса, зарегистрированного в модели) для удаления из БД.
//
Процедура УдалитьСущность(ОбъектМодели, Сущность)

	ИмяТаблицы = ОбъектМодели.ИмяТаблицы();

	Таблица = КешТаблиц.Получить(ИмяТаблицы);

	Если Таблица = Неопределено Тогда
		ВызватьИсключение "Таблица " + ИмяТаблицы + " не найдена";
	КонецЕсли;

	Идентификатор = ОбъектМодели.ПолучитьЗначениеИдентификатора(Сущность);
	Если ТипЗнч(Идентификатор) = Тип("Число") Тогда
		Идентификатор = Формат(Идентификатор, "ЧГ=");
	КонецЕсли;

	СтрокаТЗ = Таблица.Найти(Идентификатор, "_Идентификатор");

	Если НЕ СтрокаТЗ = Неопределено Тогда
		Таблица.Удалить(СтрокаТЗ);
	КонецЕсли;

КонецПроцедуры

// Осуществляет поиск строк в таблице по указанному отбору.
//
// Параметры:
//   ОбъектМодели - ОбъектМодели - Объект, содержащий описание класса-сущности и настроек таблицы БД.
//   ОпцииПоиска - ОпцииПоиска - Опции поиска. Содержит следующие параметры:
//     * Отборы - Массив - Отбор для поиска. Каждый элемент массива должен иметь тип "ЭлементОтбора".
//                         Каждый элемент отбора преобразуется к условию поиска.
//                         В качестве "ПутьКДанным" указываются имена колонок.
//     * Сортировки - Массив - Сортировка для результата поиска.
//                             Каждый элемент массива должен иметь тип "ЭлементПорядка".
//                             В качестве "ПутьКДанным" указываются имена колонок.
//
//  Возвращаемое значение:
//   Массив - Массив, элементами которого являются "Соответствия". Ключом элемента соответствия является имя колонки,
//     значением элемента соответствия - значение колонки.
//
Функция НайтиСтрокиВТаблице(ОбъектМодели, ОпцииПоиска) Экспорт

	Событие = НачатьЗапрос("SELECT", ОбъектМодели);
	Блокировка.Чтение().Заблокировать();

	Попытка
		Результат = ПрочитатьСтрокиТаблицы(ОбъектМодели, ОпцииПоиска);
	Исключение
		Блокировка.Чтение().Разблокировать();
		ЗавершитьЗапрос(Событие, ИнформацияОбОшибке());
		ВызватьИсключение;
	КонецПопытки;

	Блокировка.Чтение().Разблокировать();
	ЗавершитьЗапрос(Событие, , Результат.Количество());

	Возврат Результат;

КонецФункции

// Читает строки таблицы кеша по указанному отбору.
//
// Параметры:
//   ОбъектМодели - ОбъектМодели - Объект, содержащий описание класса-сущности и настроек таблицы БД.
//   ОпцииПоиска - ОпцииПоиска - Опции поиска. См. НайтиСтрокиВТаблице.
//
//  Возвращаемое значение:
//   Массив - Массив, элементами которого являются "Соответствия". Ключом элемента соответствия является имя колонки,
//     значением элемента соответствия - значение колонки.
//
Функция ПрочитатьСтрокиТаблицы(ОбъектМодели, ОпцииПоиска)

	ИмяТаблицы = ОбъектМодели.ИмяТаблицы();
	Отборы = ОпцииПоиска.Отборы();
	Сортировки = ОпцииПоиска.Сортировки();

	Таблица = КешТаблиц.Получить(ИмяТаблицы);

	Если Таблица = Неопределено Тогда
		ВызватьИсключение "Таблица " + ИмяТаблицы + " не найдена";
	КонецЕсли;

	ПроцессорКоллекций = ПроцессорыКоллекций.ИзКоллекции(Таблица);

	Колонки = ОбъектМодели.Колонки();
	
	Для Каждого ЭлементОтбора Из Отборы Цикл

		НайденныеКолонки = Колонки.НайтиСтроки(Новый Структура("ИмяКолонки", ЭлементОтбора.ПутьКДанным));

		Если НайденныеКолонки.Количество() = 0 Тогда
			ИмяПоля = ЭлементОтбора.ПутьКДанным;
		Иначе
			ИмяПоля = НайденныеКолонки[0].ИмяПоля;
		КонецЕсли;

		Если ЭлементОтбора.ВидСравнения = ВидСравнения.В Тогда
			СтрокаУсловие = СтрШаблон(
				"Элемент -> Значение.Найти(Элемент._Сущность.%1) <> Неопределено",
				ЭлементОтбора.ПутьКДанным
			);
		ИначеЕсли ЭлементОтбора.ВидСравнения = ВидСравнения.НеВ Тогда
			СтрокаУсловие = СтрШаблон(
				"Элемент -> Значение.Найти(Элемент._Сущность.%1) = Неопределено",
				ЭлементОтбора.ПутьКДанным
			);
		Иначе
			СтрокаУсловие = СтрШаблон(
				"Элемент -> Элемент._Сущность.%1 %2 Значение",
				ИмяПоля,
				ЭлементОтбора.ВидСравнения
			);
		КонецЕсли;

		ДополнительныеПараметры = Новый Структура("Значение", ЭлементОтбора.Значение);
		ПроцессорКоллекций = ПроцессорКоллекций.Фильтровать(СтрокаУсловие, ДополнительныеПараметры);
	КонецЦикла;

	СтрокаСортировка = "";
	Для Каждого ЭлементПорядка Из Сортировки Цикл

		НайденныеКолонки = Колонки.НайтиСтроки(Новый Структура("ИмяКолонки", ЭлементПорядка.ПутьКДанным));

		Если НайденныеКолонки.Количество() = 0 Тогда
			ИмяПоля = ЭлементПорядка.ПутьКДанным;
		Иначе
			ИмяПоля = НайденныеКолонки[0].ИмяПоля;
		КонецЕсли;
		ПорядокСортировки = ?(ЭлементПорядка.НаправлениеСортировки = НаправлениеСортировки.Возр,
			"ПрямойПорядок", 
			"ОбратныйПорядок"
		);

		// формирование текста лямбды для сравнения элементов по нескольким полям
		СтрокаСортировка = СтрокаСортировка + СтрШаблон(
			"Сравнение = ПроцессорыКоллекцийСлужебный.СравнениеЗначений%1(Элемент1._Сущность.%2, Элемент2._Сущность.%2);
			|Если Сравнение <> 0 Тогда
			|	Возврат Сравнение;
			|КонецЕсли;
			|
			|",
			ПорядокСортировки,
			ИмяПоля
		);

	КонецЦикла;

	Если СтрокаСортировка <> "" Тогда
		СтрокаСортировка = "(Элемент1, Элемент2) -> " + Символы.ПС + СтрокаСортировка + "Возврат Сравнение;";

		ПроцессорКоллекций = ПроцессорКоллекций.Сортировать(СтрокаСортировка);
	КонецЕсли;

	Если ОпцииПоиска.ВыбираетсяСоСмещением() <> Неопределено Тогда
		ПроцессорКоллекций = ПроцессорКоллекций.Пропустить(ОпцииПоиска.ВыбираетсяСоСмещением());
	КонецЕсли;

	Если ОпцииПоиска.ВыбираютсяПервые() <> Неопределено Тогда
		ПроцессорКоллекций = ПроцессорКоллекций.Первые(ОпцииПоиска.ВыбираютсяПервые());
	КонецЕсли;

	ДанныеТаблицы = ПроцессорКоллекций.ВМассив();

	Результат = Новый Массив();

	Колонки = ОбъектМодели.Колонки();
	
	Для Каждого СтрокаДанныхТаблицы Из ДанныеТаблицы Цикл
		ЗначенияКолонок = Новый Соответствие;
		Для Каждого Колонка Из Колонки Цикл
			Значение = Рефлектор.ПолучитьСвойство(СтрокаДанныхТаблицы._Сущность, Колонка.ИмяПоля);
			ЗначенияКолонок.Вставить(Колонка.ИмяКолонки, Значение);
		КонецЦикла;
		Результат.Добавить(ЗначенияКолонок);
	КонецЦикла;

	Возврат Результат;

КонецФункции

// Удаляет строки в таблице по указанному отбору.
//
// Параметры:
//   ОбъектМодели - ОбъектМодели - Объект, содержащий описание класса-сущности и настроек таблицы БД.
//   ОпцииПоиска - ОпцииПоиска - Опции поиска. Содержит следующие параметры:
//     * Отборы - Массив - Отбор для поиска. Каждый элемент массива должен иметь тип "ЭлементОтбора".
//                         Каждый элемент отбора преобразуется к условию поиска.
//                         В качестве "ПутьКДанным" указываются имена колонок.
//
Процедура УдалитьСтрокиВТаблице(ОбъектМодели, ОпцииПоиска) Экспорт

	Событие = НачатьЗапрос("DELETE", ОбъектМодели);
	Блокировка.Запись().Заблокировать();

	Попытка
		УдалитьНайденныеСтроки(ОбъектМодели, ОпцииПоиска);
	Исключение
		Блокировка.Запись().Разблокировать();
		ЗавершитьЗапрос(Событие, ИнформацияОбОшибке());
		ВызватьИсключение;
	КонецПопытки;

	Блокировка.Запись().Разблокировать();
	ЗавершитьЗапрос(Событие);

КонецПроцедуры

// Удаляет строки таблицы кеша по указанному отбору.
//
// Параметры:
//   ОбъектМодели - ОбъектМодели - Объект, содержащий описание класса-сущности и настроек таблицы БД.
//   ОпцииПоиска - ОпцииПоиска - Опции поиска. См. НайтиСтрокиВТаблице.
//
Процедура УдалитьНайденныеСтроки(ОбъектМодели, ОпцииПоиска)

	СтрокиКУдалению = ПрочитатьСтрокиТаблицы(ОбъектМодели, ОпцииПоиска);
	Если СтрокиКУдалению.Количество() > 0 Тогда

		ИмяКолонкиИдентификатора = ОбъектМодели.Идентификатор().ИмяКолонки;
		
		ИмяТаблицы = ОбъектМодели.ИмяТаблицы();
		Таблица = КешТаблиц.Получить(ИмяТаблицы);

		Для Каждого СтрокаКУдалению Из СтрокиКУдалению Цикл
			Идентификатор = СтрокаКУдалению.Получить(ИмяКолонкиИдентификатора);
			Если ТипЗнч(Идентификатор) = Тип("Число") Тогда
				Идентификатор = Формат(Идентификатор, "ЧГ=");
			КонецЕсли;

			СтрокаТЗ = Таблица.Найти(Идентификатор, "_Идентификатор");

			Если НЕ СтрокаТЗ = Неопределено Тогда
				Таблица.Удалить(СтрокаТЗ);
			КонецЕсли;
		КонецЦикла;

	КонецЕсли;
КонецПроцедуры

// Выдает следующий идентификатор таблицы.
//
// Идентификаторы выдаются счетчиком, а не поиском максимума по таблице: перебор стоил бы
// полного прохода на каждую вставку, а массовая вставка была бы квадратичной.
//
// Параметры:
//   ИмяТаблицы - Строка - Имя таблицы, для которой выдается идентификатор.
//
// Возвращаемое значение:
//   Число - Идентификатор, не выдававшийся ранее.
//
Функция СледующийИдентификатор(Знач ИмяТаблицы)

	ПоследнийИдентификатор = СчетчикиИдентификаторов.Получить(ИмяТаблицы);
	Если ПоследнийИдентификатор = Неопределено Тогда
		ПоследнийИдентификатор = 0;
	КонецЕсли;

	Результат = ПоследнийИдентификатор + 1;
	СчетчикиИдентификаторов.Вставить(ИмяТаблицы, Результат);

	Возврат Результат;

КонецФункции

// Учитывает в счетчике идентификатор сохраняемой сущности.
//
// Идентификатор мог быть задан вызывающим кодом и оказаться больше выданных счетчиком. Тогда
// следующая генерация обязана продолжить с него, иначе вернет занятое значение и запись затрет
// чужую строку.
//
// Параметры:
//   ИмяТаблицы - Строка - Имя таблицы, в которую сохраняется сущность.
//   Идентификатор - Произвольный - Значение идентификатора сохраняемой сущности.
//
Процедура УчестьВыданныйИдентификатор(Знач ИмяТаблицы, Знач Идентификатор)

	Если ТипЗнч(Идентификатор) <> Тип("Число") Тогда
		Возврат;
	КонецЕсли;

	ПоследнийИдентификатор = СчетчикиИдентификаторов.Получить(ИмяТаблицы);
	Если ПоследнийИдентификатор = Неопределено ИЛИ ПоследнийИдентификатор < Идентификатор Тогда
		СчетчикиИдентификаторов.Вставить(ИмяТаблицы, Идентификатор);
	КонецЕсли;

КонецПроцедуры
